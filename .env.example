# Environment Configuration for Cab Booking Mobile
# Variables with EXPO_PUBLIC_ prefix are embedded at build time and accessible via process.env in the app

# Your backend server IP address (use your machine's LAN IP)
EXPO_PUBLIC_IP_ADDRESS=192.168.1.57

# API Gateway URL (single entry point for all services)
EXPO_PUBLIC_GATEWAY_PORT=8080
